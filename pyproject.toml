[project]
name = "project-crud-back-front-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.1"
dependencies = [
    "sqlalchemy>=2.0.40",
]
